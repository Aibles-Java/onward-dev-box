# Session: 2026-07-04

## Harness wave 2: issue/PR skills ported from feature_flag

- Compared both repos' `.claude` trees: hooks + save-memory already identical;
  git-workflow/settings/drift-monitor differ deliberately (infra adaptations).
- Ported with adaptation: `create-pr`, `estimate-issue` + calibration.md,
  `issue-workflow`, `scripts/issue-board.sh`, `.github/PULL_REQUEST_TEMPLATE.md`.
- Verified: shellcheck clean, arg-validation paths, read-only board round-trip
  (`status 18` → "Todo"; board #3 confirmed to host 17 onward-dev-box cards).
- Recorded as decision 0002; branch `feature/port-harness-skills` → PR to develop.

## Issue #2: docker-compose.yml (Phase 1, item 1.1)

- Worked via issue-workflow: board card → In progress, branch
  `feature/issue-2-docker-compose` off origin/develop.
- Authored compose file (postgres/sonarqube/adminer, profiles core/quality/tools,
  `onward` network, named volumes) + `postgres/init/.gitkeep` mount target.
- Verified all acceptance live on POSTGRES_PORT=5433 (5432 held by old
  ff_postgres): core healthy, quality Healthy with manually seeded sonar db,
  config -q clean, adminer HTTP 200. Gotcha: sonarqube:community lacks wget —
  healthcheck switched to curl. Scratch env removed with `down -v`.
- Recorded as decision 0003; PR to develop next.
