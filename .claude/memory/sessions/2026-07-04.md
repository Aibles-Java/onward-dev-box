# Session: 2026-07-04

## Harness wave 2: issue/PR skills ported from feature_flag

- Compared both repos' `.claude` trees: hooks + save-memory already identical;
  git-workflow/settings/drift-monitor differ deliberately (infra adaptations).
- Ported with adaptation: `create-pr`, `estimate-issue` + calibration.md,
  `issue-workflow`, `scripts/issue-board.sh`, `.github/PULL_REQUEST_TEMPLATE.md`.
- Verified: shellcheck clean, arg-validation paths, read-only board round-trip
  (`status 18` → "Todo"; board #3 confirmed to host 17 onward-dev-box cards).
- Recorded as decision 0002; branch `feature/port-harness-skills` → PR to develop.

## Issue #2: docker-compose.yml (Phase 1, item 1.1)

- Worked via issue-workflow: board card → In progress, branch
  `feature/issue-2-docker-compose` off origin/develop.
- Authored compose file (postgres/sonarqube/adminer, profiles core/quality/tools,
  `onward` network, named volumes) + `postgres/init/.gitkeep` mount target.
- Verified all acceptance live on POSTGRES_PORT=5433 (5432 held by old
  ff_postgres): core healthy, quality Healthy with manually seeded sonar db,
  config -q clean, adminer HTTP 200. Gotcha: sonarqube:community lacks wget —
  healthcheck switched to curl. Scratch env removed with `down -v`.
- Recorded as decision 0003; PR to develop next.

## Issue #3: postgres/init first-boot SQL (Phase 1, item 1.2)

- Estimated S/3h (logged in calibration.md; human AFK — recommended value
  written, adjustable). Worked via issue-workflow: card → In progress, branch
  `feature/issue-3-postgres-init-sql` off origin/develop (issue #2 merged).
- Authored `postgres/init/01-init-databases.sql` idempotently (DO-block roles,
  `\gexec` databases) with contract credentials verbatim; removed `.gitkeep`.
- Verified live on POSTGRES_PORT=5433: fresh first boot, TCP+password `select 1`
  as ff_user and sonar, idempotent re-apply is a no-op, feature_flag booted via
  `SPRING_DATASOURCE_URL` override — 8 Liquibase changesets, Tomcat on 8081.
  Scratch env removed with `down -v`.
- Recorded as decision 0004; PR to develop next.

## Issue #4: .env.example (Phase 1, item 1.3)

- Worked via issue-workflow: card → In progress, branch
  `feature/issue-4-env-example` off origin/develop (issue #3 merged;
  develop advanced). Estimate S/1.5h drafted; human AFK at confirmation so it was
  NOT written to the board (estimate-issue forbids unconfirmed writes) — pending.
- Authored `.env.example`: all 11 compose `${VAR}`s with defaults identical to
  compose fallbacks, local-only credential warning, cross-repo note that
  POSTGRES_PORT=5432 is the feature_flag contract and per-service creds live in
  init SQL. Parameterized remaining literal image tags (POSTGRES_TAG, ADMINER_TAG).
- Verified: `cp .env.example .env && docker compose config -q` clean across all
  profiles, compose var set ≡ documented keys (diff), rendered images unchanged,
  config also clean with no `.env`. Test `.env` removed.
- Recorded as decision 0005; PR to develop next.
