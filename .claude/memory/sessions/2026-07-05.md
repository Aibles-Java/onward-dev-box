# 2026-07-05

Worked issue #9 (Phase 1 acceptance verification, clean-machine flow). No code
changes required — this was pure verification. Confirmed on this machine:
`make init && make up && make run` boots from a clean state with zero manual
steps (postgres healthy, Liquibase applies 8 changesets, app responds 200 on
8081); `make nuke && make up` recreates a pristine DB and Liquibase re-applies
cleanly; `doctor.sh` catches all three required failure modes (Docker down,
port 5432 taken, wrong Java version) via non-destructive PATH-stubbed
`docker`/`java` binaries; no secrets committed and `shellcheck` passes clean.
Ticked the four Phase 1 boxes in `docs/INFRASTRUCTURE.md` §6. Details in
`decisions/0010-verify-phase1-clean-machine.md`.

## Session 2 (2026-07-05)

Worked issue #28 (feature PRs merging into `develop` never auto-close their
linked issue, since GitHub's closing keywords only fire on merge to the
default branch `main`). Chose option 1: `issue-board.sh done` now runs
`gh issue close` explicitly right after moving the board card to Done, so one
command handles both. Documented the gap and the decision in `issue-workflow`
SKILL.md's new "Closing issues" section. Details in
`decisions/0011-close-issue-on-done.md`.

## Session 3 (2026-07-05)

Worked issue #10 (Phase 2, item 2.1): added `sonarqube/bootstrap.sh` — waits
for SonarQube UP via `wait-for.sh`, rotates the default admin password,
creates the `aibles:feature_flag` project, and (re)generates `SONAR_TOKEN`
into `.env`. Added `SONAR_ADMIN_PASSWORD` to `.env.example`. Verified live
against a real `quality`-profile SonarQube container (fresh run + idempotent
re-run). Found and fixed two bugs during verification: SonarQube's password
policy needs upper+lower+digit, and `curl --data-urlencode` silently switches
to POST unless `-G` is passed (broke the idempotency check). Quality-gate
creation (step 5) intentionally deferred to a follow-up issue. Details in
`decisions/0012-sonar-bootstrap.md`.

## Session 4 (2026-07-05)

Worked issue #11, the quality-gate follow-up from #10's step 5. Added
`sonarqube/quality-gate.json` (checked-in DEV/SIT-shared gate: `new_coverage`
ratchet at 0 → target 80%, `new_blocker_violations` at 0) and extended
`sonarqube/bootstrap.sh` to create/reconcile a named gate from that file and
assign it (not default) to `aibles:feature_flag`. Verified live: discovered
SonarQube auto-adds extra "Clean as You Code" conditions to any newly created
custom gate, invisible in the create response — added a prune step so the
checked-in JSON stays authoritative. Confirmed a clean idempotent re-run.
Details in `decisions/0013-quality-gate-json.md`.

## Session 5 (2026-07-05)

Asked to work issue #13 (Phase 2 acceptance verification); found its
acceptance criteria depend on `make sonar`, which didn't exist yet (tracked
separately as open issue #12) — implemented #12 first, then verified #13
against it. Added a `sonar:` Makefile target running `./mvnw verify
sonar:sonar` against `../feature_flag`, with `-Dsonar.qualitygate.wait=true`
added beyond the issue's literal command so the gate verdict actually prints
and gates the exit code, plus guards for a missing checkout / missing
`SONAR_TOKEN`. Verified end-to-end against a live `quality`-profile stack:
`make up-all` healthy, `bootstrap.sh` re-run idempotently, `make sonar`
printed `QUALITY GATE STATUS: PASSED` and exited 0; both error guards
manually triggered. Details in `decisions/0014-make-sonar-target.md`.
