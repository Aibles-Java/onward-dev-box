# 2026-07-05

Worked issue #9 (Phase 1 acceptance verification, clean-machine flow). No code
changes required — this was pure verification. Confirmed on this machine:
`make init && make up && make run` boots from a clean state with zero manual
steps (postgres healthy, Liquibase applies 8 changesets, app responds 200 on
8081); `make nuke && make up` recreates a pristine DB and Liquibase re-applies
cleanly; `doctor.sh` catches all three required failure modes (Docker down,
port 5432 taken, wrong Java version) via non-destructive PATH-stubbed
`docker`/`java` binaries; no secrets committed and `shellcheck` passes clean.
Ticked the four Phase 1 boxes in `docs/INFRASTRUCTURE.md` §6. Details in
`decisions/0010-verify-phase1-clean-machine.md`.

## Session 2 (2026-07-05)

Worked issue #28 (feature PRs merging into `develop` never auto-close their
linked issue, since GitHub's closing keywords only fire on merge to the
default branch `main`). Chose option 1: `issue-board.sh done` now runs
`gh issue close` explicitly right after moving the board card to Done, so one
command handles both. Documented the gap and the decision in `issue-workflow`
SKILL.md's new "Closing issues" section. Details in
`decisions/0011-close-issue-on-done.md`.
