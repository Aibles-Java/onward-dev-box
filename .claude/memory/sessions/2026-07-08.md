# Session — 2026-07-08

Implemented issue #14: `scripts/seed-feature-flag.sh` (Phase 3 developer
convenience), wired as `make seed`. Explored `../feature_flag`'s Postman
collection to reverse-engineer the Admin API request/response shapes for
register/login/org/project/environment/flag, since no OpenAPI spec was
available locally.

Discovered two gaps between the Postman collection and the live app: (1)
`POST /api/v1/auth/register` currently returns `201` with an empty body (no
token), unlike what the collection's test script assumes — the script always
follows with a login call; (2) SDK evaluation auth header is
`X-Environment-Key`, not `X-API-Key`. Also hit a macOS bash 3.2 incompatibility
(`declare -A` not supported) caught only by actually running the script, not
by shellcheck.

Verified full clean-machine loop: `make nuke` → `make up` → `make run` →
`make seed` (fresh success, working SDK key confirmed via curl) → `make seed`
again (correctly failed with "already seeded", no duplicates). Details in
`decisions/0016-seed-feature-flag.md`. Not yet pushed/PR'd.
